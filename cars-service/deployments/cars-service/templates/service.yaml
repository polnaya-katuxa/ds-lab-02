apiVersion: v1
kind: Service
metadata:
  name: {{ include "cars-service.fullname" . }}
  labels:
    {{- include "cars-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cars-service.selectorLabels" . | nindent 4 }}
