apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cars-service.fullname" . }}-test-connection"
  labels:
    {{- include "cars-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cars-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
