apiVersion: v1
kind: Service
metadata:
  name: {{ include "rental-service.fullname" . }}
  labels:
    {{- include "rental-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rental-service.selectorLabels" . | nindent 4 }}
